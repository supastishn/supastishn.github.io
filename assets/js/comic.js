document.addEventListener('DOMContentLoaded', function() {
  // This script is kept for potential future use or compatibility,
  // but the primary comic rendering logic has been moved to the inline script 
  // in index.markdown (or potentially _layouts/comic.html if used there).
  console.log("assets/js/comic.js: DOM fully loaded and parsed. No comic rendering tasks performed by this script.");

  // Intentionally leaving the rendering and URL parameter logic out, 
  // as it's now handled within the main page's inline script.

});
